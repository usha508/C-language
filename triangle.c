#include <stdio.h>
int main() {
    int b,h;
    printf("enter base length:");
    scanf("%d", &b);
    printf("enter height length:");
    scanf("%d", &h);
    printf("area of the triangle is %d", (b*h)/2);
    return 0;
}