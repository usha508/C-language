#include <stdio.h>
int main() {
    float b,h;
    printf("enter base length:");
    scanf("%f", &b);
    printf("enter height length:");
    scanf("%f", &h);
    printf("the area of parallelogram is:%.2f", b*h);
}